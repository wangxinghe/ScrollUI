<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mouxuejie.scrollui" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RecyclerVSListActivity"/>
        <activity android:name=".page.ListViewActivity"/>
        <activity android:name=".page.RecyclerViewActivity"/>
        <activity android:name=".LinearActivity"/>
        <activity android:name=".GridActivity"/>
        <activity android:name=".StaggeredGridActivity"/>
        <activity android:name=".imageview.ImageActivity"/>
        <activity android:name=".scrollview.ScrollViewActivity"/>
        <activity android:name=".scrollview.MyScrollViewActivity"/>
        <activity android:name=".canvas.CanvasActivity"/>
        <activity android:name=".rsa.RSAActivity"/>
    </application>

</manifest>